<% if page.current? %>
  <li class='active'>
    <%= content_tag :a, page, method: :get, rel: (page.next? ? 'next' : (page.prev? ? 'prev' : nil)) %>
  </li>
<% else %>
  <li>
    <%= link_to page, url, method: :get, rel: (page.next? ? 'next' : (page.prev? ? 'prev' : nil)) %>
  </li>
<% end %>